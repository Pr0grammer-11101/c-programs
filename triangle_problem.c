#include<stdio.h>



int main(void)
{
	int num, size;
	
	printf("How many triangles?");
	scanf("%d", &num);
	size = num*3;

	int triangles[size];
	
	printf("Enter the sides into the triangles:\n");
	int i=0, repeat=0;

	while(repeat<num)
	{
		printf("Triangle %d\n");

		while(i<size)


	return 0;
}
